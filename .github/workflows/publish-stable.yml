name: Publish Stable

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  publish:
    uses: nano-step/shared-workflows/.github/workflows/publish-stable.yml@v1
    with:
      package-name: "open-design-mcp"
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
