/**
 * od-client: HTTP wrapper for the Open Design daemon.
 * Implemented in the BYOK pipeline change (separate OpenSpec change).
 * Exported as a typed placeholder so the module is non-empty and
 * downstream consumers can typecheck against a stable identifier.
 */
export const OD_CLIENT_PLACEHOLDER = 'not-yet-implemented' as const;
